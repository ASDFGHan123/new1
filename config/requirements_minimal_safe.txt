# Minimal safe requirements for Python 3.13
# Focus on essential packages only
Django>=4.2,<5.0
djangorestframework>=3.14.0
django-cors-headers>=4.0.0
python-decouple>=3.8
requests>=2.31.0
pytz>=2023.3

# Note: These packages are intentionally excluded due to compilation/3.13 issues:
# - Pillow (image processing)
# - cryptography (security)
# - psycopg2-binary (PostgreSQL)
# - channels (WebSocket)
# - django-compressor (CSS/JS)
# - celery (background tasks)